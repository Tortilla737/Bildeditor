Fragen
1. Welchen Inhalt hat die Datei .gitignore für Java-Projekte?
*.class
2. Welcher Befehl erstellt eine lokale Kopie eines Remote-Repository?
git clone <url>
3. Welcher Befehl übernimmt Änderungen aus github-Repository ins lokale Repository?
git pull
4. Welche Befehlsfolge ist notwendig, um einen lokalen Stand zu veröffentlichen, wenn sich das
github-Repository verändert hat.
git push
5. Wie werden lokale Branches ins github-Repository übertragen?

6. Wie werden lokale Tags ins github-Repository übertragen?

7. Nach einem git pull Aufruf werden Konflikte angezeigt. Wie können Sie sich anzeigen lassen,
welche Dateien betroffen sind?

8. Kann git ohne github verwendet werden?
Ja